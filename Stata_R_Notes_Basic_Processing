Stata 
*regular expressions*

regexm()

Stata only uses regular expressions w/ strings 
Specifically: only to be used to ASCII characters.  See: https://www.stata.com/help.cgi?regexm() 

[] brackets for "a set of allowable characters/expressions to use in matching"
() parentheses for matching and denoting "a subexpression group"
  More here: https://www.stata.com/support/faqs/data-management/regular-expressions/
  
You can use regexm() with replace() 
replace() for replacing the values of an existing variable 
